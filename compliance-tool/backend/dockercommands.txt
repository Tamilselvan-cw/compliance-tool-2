#step1 

docker rm -f competencymapper-api 2>/dev/null || true
docker rmi competencymapper-api:v1 2>/dev/null || true
docker system prune -af -f

docker build --no-cache -t competencymapper-api:v1 .

docker save -o competencymapper_api_v1.tar competencymapper-api:v1

#step 2

ssh root@77.37.47.181
CWroot@tech1722

#step3

cd /var/www/skillmapper/backend/

sudo docker rm -f skillmapper-api

sudo docker rm -f competencymapper-api skillmapper-api 2>/dev/null || true

sudo docker image rm competencymapper-api:v1 2>/dev/null || true

sudo docker load -i /var/www/skillmapper/backend/competencymapper_api_v1.tar

sudo docker run -d --name competencymapper-api \
  --network host \
  --env-file /var/www/skillmapper/backend/.env.docker \
  --restart unless-stopped \
  competencymapper-api:v1 \
  uvicorn app.main:app --host 0.0.0.0 --port 19083


sudo docker logs -f competencymapper-api

#####

77.37.47.181
root
CWroot@tech1722
22